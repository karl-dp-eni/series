{% extends 'base.html.twig' %}

{% block body %}
    {% if app.user %}
        <h1>Bienvenue {{ app.user.firstname }}</h1>
    {% else %}
        <h1>Bienvenue Guest</h1>
    {% endif %}
{% endblock %}

{% block title %}
    {{ parent() }} | Home
{% endblock %}
