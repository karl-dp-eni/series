<nav>
    <ul>
        {% if app.user %}
            <li><a href="{{ path('main_home') }}">Home</a></li>
            <li><a href="{{ path('main_test') }}">Test</a></li>
            <li><a href="{{ path('serie_list') }}">All TV-Shows</a></li>
            <li><a href="{{ path('serie_create') }}">Create a TV-Shows</a></li>
            {% if is_granted("ROLE_ADMIN") %}
                <li><a href="{{ path('season_create') }}">Add a season</a></li>
            {% endif %}
            <li><a href="{{ path('app_logout') }}">Logout</a></li>
        {% else %}
            <li><a href="{{ path('app_login') }}">Sign in</a></li>
            <li><a href="{{ path('app_register') }}">Register</a></li>
        {% endif %}
    </ul>
</nav>
